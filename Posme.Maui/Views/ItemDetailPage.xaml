<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="{Binding Title}"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="true"
             x:Class="Posme.Maui.Views.ItemDetailPage">
    <ContentPage.Content>
        <StackLayout Spacing="5" Padding="15">
            <Label Text="Codigo de Barra:" FontFamily="Roboto" Style="{StaticResource PrimaryText}"/>
            <Label Text="{Binding BarCode}" FontFamily="Roboto" Margin="0, 0, 0, 15"/>
            <Label Text="Codigo:" FontFamily="Roboto" Style="{StaticResource PrimaryText}" />
            <Label Text="{Binding ItemNumber}" FontFamily="Roboto" Margin="0, 0, 0, 15"/>
            <Label Text="Descripcion:" FontFamily="Roboto" Style="{StaticResource PrimaryText}" />
            <Label Text="{Binding Name}" FontFamily="Roboto" Margin="0, 0, 0, 15"/>
            <Label Text="Precio:" FontFamily="Roboto" Style="{StaticResource PrimaryText}" />
            <Label Text="{Binding PrecioPublico}" FontFamily="Roboto" Margin="0, 0, 0, 15"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>